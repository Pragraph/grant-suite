// tRPC API route handler — will be configured in a future session
export async function GET() {
  return new Response("tRPC not configured yet", { status: 501 });
}

export async function POST() {
  return new Response("tRPC not configured yet", { status: 501 });
}
